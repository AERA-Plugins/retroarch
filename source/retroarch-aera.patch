diff --git a/Makefile.common b/Makefile.common
index 57dabf5..8745901 100644
--- a/Makefile.common
+++ b/Makefile.common
@@ -349,0 +350,8 @@ OBJ += \
+ifeq ($(HAVE_AERA), 1)
+   DEFINES += -DHAVE_AERA
+   OBJ += aera/aera_platform.o \
+          gfx/drivers/aera_gfx.o \
+          input/drivers/aera_input.o \
+          audio/drivers/aera_audio.o
+endif
+
diff --git a/audio/audio_driver.c b/audio/audio_driver.c
index a4a28cf..2d442be 100644
--- a/audio/audio_driver.c
+++ b/audio/audio_driver.c
@@ -105,0 +106,3 @@ audio_driver_t *audio_drivers[] = {
+#ifdef HAVE_AERA
+   &audio_aera,
+#endif
diff --git a/configuration.c b/configuration.c
index 77abe35..a99c1df 100644
--- a/configuration.c
+++ b/configuration.c
@@ -762 +762,3 @@ static const enum location_driver_enum LOCATION_DEFAULT_DRIVER = LOCATION_NULL;
-#if (defined(_3DS) || defined(DINGUX)) && defined(HAVE_RGUI)
+#if defined(HAVE_AERA) && defined(HAVE_MATERIALUI)
+static const enum menu_driver_enum MENU_DEFAULT_DRIVER = MENU_MATERIALUI;
+#elif (defined(_3DS) || defined(DINGUX)) && defined(HAVE_RGUI)
@@ -3488,0 +3491,5 @@ static bool check_menu_driver_compatibility(settings_t *settings)
+
+#ifdef HAVE_AERA
+   if (string_is_equal(video_driver, "aera"))
+      return true;
+#endif
diff --git a/audio/audio_driver.h b/audio/audio_driver.h
index cd0ca56..6bf5f93 100644
--- a/audio/audio_driver.h
+++ b/audio/audio_driver.h
@@ -429,0 +430 @@ extern audio_driver_t audio_audioworklet;
+extern audio_driver_t audio_aera;
diff --git a/gfx/video_driver.c b/gfx/video_driver.c
index 7917922..c9f6614 100644
--- a/gfx/video_driver.c
+++ b/gfx/video_driver.c
@@ -344,0 +345,3 @@ const video_driver_t *video_drivers[] = {
+#ifdef HAVE_AERA
+   &video_aera,
+#endif
diff --git a/gfx/font_driver.c b/gfx/font_driver.c
index c55f9fa..a2b1511 100644
--- a/gfx/font_driver.c
+++ b/gfx/font_driver.c
@@ -85,0 +86,14 @@ static bool font_init_first(
+#ifdef HAVE_AERA
+      case FONT_DRIVER_RENDER_AERA:
+         {
+            void *data = aera_font.init(video_data, font_path, font_size,
+                  is_threaded);
+            if (data)
+            {
+               *font_driver = &aera_font;
+               *font_handle = data;
+               return true;
+            }
+         }
+         break;
+#endif
diff --git a/gfx/font_driver.h b/gfx/font_driver.h
index ab849b0..99554c0 100644
--- a/gfx/font_driver.h
+++ b/gfx/font_driver.h
@@ -159,0 +160 @@ extern font_renderer_t rsx_font;
+extern font_renderer_t aera_font;
diff --git a/gfx/gfx_display.c b/gfx/gfx_display.c
index 85a207c..aa28152 100644
--- a/gfx/gfx_display.c
+++ b/gfx/gfx_display.c
@@ -50,0 +51,3 @@ static gfx_display_ctx_driver_t *gfx_display_ctx_drivers[] = {
+#ifdef HAVE_AERA
+   &gfx_display_ctx_aera,
+#endif
diff --git a/gfx/gfx_display.h b/gfx/gfx_display.h
index 79aac74..ec9c4ea 100644
--- a/gfx/gfx_display.h
+++ b/gfx/gfx_display.h
@@ -113 +113,2 @@ enum gfx_display_driver_type
-   GFX_VIDEO_DRIVER_RSX
+   GFX_VIDEO_DRIVER_RSX,
+   GFX_VIDEO_DRIVER_AERA
@@ -340,0 +342 @@ extern gfx_display_ctx_driver_t gfx_display_ctx_rsx;
+extern gfx_display_ctx_driver_t gfx_display_ctx_aera;
diff --git a/gfx/video_driver.h b/gfx/video_driver.h
index ca6434c..da7fa66 100644
--- a/gfx/video_driver.h
+++ b/gfx/video_driver.h
@@ -1367,0 +1368 @@ extern video_driver_t video_network;
+extern video_driver_t video_aera;
diff --git a/gfx/video_defines.h b/gfx/video_defines.h
index 34ebb43..8c6822e 100644
--- a/gfx/video_defines.h
+++ b/gfx/video_defines.h
@@ -150 +150,2 @@ enum font_driver_render_api
-   FONT_DRIVER_RENDER_RSX
+   FONT_DRIVER_RENDER_RSX,
+   FONT_DRIVER_RENDER_AERA
diff --git a/input/input_driver.c b/input/input_driver.c
--- a/input/input_driver.c
+++ b/input/input_driver.c
@@ -297,0 +298,3 @@ input_device_driver_t *joypad_drivers[] = {
+#ifdef HAVE_AERA
+   &aera_joypad,
+#endif
@@ -302,0 +303,3 @@ input_driver_t *input_drivers[] = {
+#ifdef HAVE_AERA
+   &input_aera,
+#endif
diff --git a/input/input_driver.h b/input/input_driver.h
--- a/input/input_driver.h
+++ b/input/input_driver.h
@@ -1205,0 +1206,2 @@ extern input_driver_t input_test;
+extern input_driver_t input_aera;
+extern input_device_driver_t aera_joypad;
